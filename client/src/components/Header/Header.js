import React from "react";

const Header = props => (
    <div className="jumbotron">
        <h3>New York Times Article Search</h3>
        <p>Enter a search term and an optional span of time and see what articles came out!</p>
    </div>
);

export default Header;